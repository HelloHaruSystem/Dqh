namespace Dqh.Game.Settings;

/// <summary>Timing for the fade-to-black transition played across a map change.</summary>
internal static class TransitionSettings
{
    /// <summary>Seconds to fade to black; the same duration is used again to fade back in.</summary>
    public const float FadeSeconds = 0.25f;
}
